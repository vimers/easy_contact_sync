org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
# Disable Gradle's virtual file-system watcher: on WSL2 its inotify-based
# watcher stalls assembleRelease for many minutes. Disabling it does not affect
# build correctness, only avoids the watcher overhead/contention.
org.gradle.vfs.watch=false
android.useAndroidX=true
android.enableJetifier=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
